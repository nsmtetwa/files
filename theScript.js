window.foo = {
    bar: function() {
        console.log("Hello from foo.bar function!");
        // Add your functionality here
    }
};

// Optionally, you can invoke the function immediately after defining it
window.foo.bar();
